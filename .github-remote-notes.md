Set your Bitbucket remote after repo exists:
- git remote add origin git@bitbucket.org:agency-ops/openclaw-skills.git
- git push -u origin main

If your workspace/user is different, replace `agency-ops` with the exact workspace key.
